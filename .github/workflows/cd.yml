name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Create version
      run: | 
        TAG=${{ github.ref_name }}
        echo "TAG=${TAG}" >> $GITHUB_ENV
        echo "SEMVER=${TAG#v}" >> $GITHUB_ENV

    - name: Publish files
      run: |
        dotnet publish ./src/GtPrax.WebApp -c Release -o ./dist/app -r linux-x64 --self-contained true /p:UseAppHost=true /p:Version=${SEMVER}

    - name: Create archive
      run: |
        cd dist
        chmod -x -R app/*.dll
        chmod -x -R app/*.so
        tar cfJ release-linux-x64.tar.xz *

    - name: Create release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: gh release create ${TAG} ./dist/release-linux-x64.tar.xz --generate-notes --title "${TAG}"
